<%= form_with(model: application) do |form| %>
  <% if application.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(application.errors.count, "error") %> prohibited this application from being saved:</h2>

      <ul>
        <% application.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :cover_letter, style: "display: block" %>
    <%= form.text_area :cover_letter %>
  </div>

  <div>
    <%= form.label :candidate_id, style: "display: block" %>
    <%= form.collection_select :candidate_id, Candidate.all, :id, :name %>
  </div>

  <div>
    <%= form.label :job_offer_id, style: "display: block" %>
    <%= form.collection_select :job_offer_id, JobOffer.all, :id, :title %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
